Web Crawl in Python, CLI application that generates internal linking report for websites.

Run main.py file with three arguments: website address, max concurrency, max pages as limit.

```bash
uv run main.py https://example.com 3 10
```

After executing the command, report.csv file will be created with information about each page.
